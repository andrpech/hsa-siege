{
  "name": "hsa-siege",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run prisma:introspection && node index.js",
    "start:dev": "npm run prisma:introspection && node index.js --watch",
    "prisma:migrate": "prisma migrate dev",
    "prisma:introspection": "node ./scripts/prisma-introspection.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.5.2",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "prisma": "^5.5.2"
  }
}
